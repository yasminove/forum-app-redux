// 0. create a new folder inside your warmUps folder and name it W6D5
// Inside this folder do the following:
// Create an HTML, CSS and JS files and connect them together
// Create two buttons one that says “Left” and one that say “Right”
// Create a button below them that says “Create”
// Create two unordered lists next to each other
// Write a jQuery function that creates a new element in the left unordered list if the “left” button was pressed before the “create” button
// and to the right unordered list if the “right” button was pressed before the “create” button
// Style the two unordered lists in CSS
